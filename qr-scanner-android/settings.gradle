pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode = RepositoriesMode.PREFER_SETTINGS
    repositories {
        google()
        mavenCentral()
    }
}

// Names the AAR: build/outputs/aar/retroxr-qr-{debug,release}.aar
rootProject.name = 'retroxr-qr'
