## VRInputMapper — stub kept alive so player_rig.tscn doesn't break.
## The old Left X + Right A attach-combo has been removed; physical retro
## controller objects plugged into system ports handle input routing instead.
class_name VRInputMapper
extends Node3D
